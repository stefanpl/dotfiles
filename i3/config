# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $win Mod4
set $alt Mod1

set $communicationWorkspace 5
set $lowerWorkspace 2
set $togglWorkspace 7
set $musicWorkspace 9

set $floatingTerminatorTitle "Ready when you are" 
set $tmuxTerminatorTitle "tmux terminal" 
set $topScreen DP2-2
set $bottomScreen DP2-3
set $laptopScreen eDP1

exec zsh -c screensOneMain

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$win to drag floating windows to their wanted position
floating_modifier $win
workspace_layout tabbed
focus_follows_mouse no


# start a terminal
bindsym $win+Return exec synapse
bindsym $win+n exec shutter -s


# kill focused window
bindsym $win+Shift+q kill
bindsym $alt+F4 kill

bindsym $win+d exec google-chrome chrome://newtab
bindsym $win+e exec "zsh -c 'bruce show-tmux-terminal'"

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $win+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $win+j focus left ; exec "zsh -c moveMouseToFocusedWindow"
bindsym $win+k focus down ; exec "zsh -c moveMouseToFocusedWindow"
bindsym $win+i focus up ; exec "zsh -c moveMouseToFocusedWindow"
bindsym $win+l focus right ; exec "zsh -c moveMouseToFocusedWindow"

# move focused window
bindsym $win+Shift+j move left
bindsym $win+Shift+k move down
bindsym $win+Shift+i move up
bindsym $win+Shift+l move right

# split in horizontal orientation
bindsym $win+h split h

# split in vertical orientation
bindsym $win+v split v

# enter fullscreen mode for the focused container
bindsym $win+m fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $win+udiaeresis layout stacking
bindsym $win+adiaeresis layout tabbed
bindsym $win+ssharp layout toggle split

# toggle tiling / floating
bindsym $win+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $win+space focus mode_toggle

# focus the parent container
bindsym $win+p focus parent
bindsym $win+shift+p focus child

# focus the child container
#bindsym $win+d focus child

# switch to workspace
bindsym Next exec playerctl next
bindsym Prior exec playerctl previous
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous
bindsym $win+o workspace next
bindsym $win+shift+o move container to workspace next ; workspace next
bindsym $win+u workspace prev
bindsym $win+shift+u move container to workspace prev ; workspace prev
bindsym $win+1 workspace 1
bindsym $win+2 workspace 2
bindsym $win+3 workspace 3
bindsym $win+4 workspace 4
bindsym $win+5 workspace 5
bindsym $win+6 workspace 6
bindsym $win+7 workspace 7
bindsym $win+8 workspace 8
bindsym $win+9 workspace 9
bindsym $win+0 workspace 10

# move focused container to workspace
bindsym $win+Shift+1 move container to workspace 1; workspace 1
bindsym $win+Shift+2 move container to workspace 2; workspace 2
bindsym $win+Shift+3 move container to workspace 3; workspace 3
bindsym $win+Shift+4 move container to workspace 4; workspace 4
bindsym $win+Shift+5 move container to workspace 5; workspace 5
bindsym $win+Shift+6 move container to workspace 6; workspace 6
bindsym $win+Shift+7 move container to workspace 7; workspace 7
bindsym $win+Shift+8 move container to workspace 8; workspace 8
bindsym $win+Shift+9 move container to workspace 9; workspace 9
bindsym $win+Shift+0 move container to workspace 10; workspace 10

# reload the configuration file
bindsym $win+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $win+Shift+r restart
# exit i3 (logs you out of your X session)
# bindsym $win+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
bindsym $win+Shift+e exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

		# Same keys as for changing focus – big resize steps
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym i resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # Arrow keys – small resize steps
        bindsym Left resize shrink width 1 px or 1 ppt
        bindsym Down resize shrink height 1 px or 1 ppt
        bindsym Up resize grow height 1 px or 1 ppt
        bindsym Right resize grow width 1 px or 1 ppt

        # back to normal: Enter or Escape
        bindsym Escape mode "default"
}

bindsym $win+shift+Return mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3status
		tray_output $topScreen
}

# Various scripts 
bindsym $win+Shift+Control+$alt+F1 exec zsh -c screensTwoMains
bindsym $win+Shift+Control+$alt+F2 exec zsh -c screensIiyama
bindsym $win+Shift+Control+$alt+F3 exec zsh -c screensLaptopOnly
bindsym XF86Display exec zsh -c screensLaptopOnly

bindsym $win+$alt+p exec i3lock -i ~/pictures/backgrounds/nvon_teppich.png

bindsym $win+y exec zsh -c 'ns "`tt --clean`"'
bindsym XF86AudioLowerVolume exec zsh -c 'ns -t 500 "🔊 Vol at `amixer -D pulse sset Master 3%- gr -o "[0-9]+%" | uniq`"'
bindsym XF86AudioRaiseVolume exec zsh -c 'ns -t 500 "🔊 Vol at `amixer -D pulse sset Master 3%+ gr -o "[0-9]+%" | uniq`"'

# Applications
bindsym $win+a exec /bin/zsh -c 'i3-msg \[class="Caja"\] focus | grep "success..true" || caja'
bindsym $win+r exec /bin/zsh -c 'i3-msg scratchpad show | grep "success..true" || terminator --title=$floatingTerminatorTitle --profile="No Exit"'
# bindsym $win+r exec /home/stefan/webdev/various-scripts/bruce-function-execution.sh i3-focus-or-open-window 'title=$floatingTerminatorTitle' 'terminator --title $floatingTerminatorTitle' >> /tmp/i3log.txt 2>&1

# Assignments 
assign [class="Slack"] $communicationWorkspace
# assign [class="REAPER"] "Schall und Rauch"
assign [title=$lowerTerminalTitle] $lowerWorkspace
for_window [class="Google-chrome" title="Toggl"] move container to workspace $togglWorkspace
for_window [title=$floatingTerminatorTitle] move scratchpad
for_window [Class="Spotify"] move container to workspace $musicWorkspace
for_window [title="Online Boxing Timer"] move container to workspace $musicWorkspace

workspace 1 output $topScreen
workspace $lowerWorkspace output $bottomScreen

# Startup applications
exec nm-applet
exec zsh -c chromeDidNotCrash
exec zsh -c start-bruce
exec --no-startup-id i3-msg 'workspace 1; exec zsh -c ~/webdev/various-scripts/start-chrome-in-debug-mode.sh > /tmp/sid.out 2>&1'
exec --no-startup-id i3-msg 'workspace 1; exec terminator --title=$tmuxTerminatorTitle --profile="Tmux Forever"'
exec --no-startup-id seafile-applet
exec terminator --title="$floatingTerminatorTitle"
# exec google-chrome --new-window https://toggl.com/app/timer
exec --no-startup-id slack

# colors
client.focused_inactive #333333 #285577 #FFFFFF #484E50 #5F676A
